function add(a: number, b: number): number {
    return a + b; // i have added this comment here
}

function subtract(a: number, b: number): number {
    return a - b; // i ave added this comment here as well
}